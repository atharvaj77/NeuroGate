apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: neurogate

resources:
- namespace.yaml
- configmap.yaml
- deployment.yaml
- service.yaml
- ingress.yaml
- hpa.yaml
- pdb.yaml
- networkpolicy.yaml
- rbac.yaml

commonLabels:
  app.kubernetes.io/name: neurogate
  app.kubernetes.io/instance: neurogate
  app.kubernetes.io/version: "1.0.0"
  app.kubernetes.io/component: ai-gateway
  app.kubernetes.io/part-of: neurogate-platform
  app.kubernetes.io/managed-by: kustomize
